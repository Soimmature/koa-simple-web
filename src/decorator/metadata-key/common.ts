const useMiddlewares = Symbol('middlewares')
const controller = Symbol('controller')
const requestPath = Symbol('path')
const requestMethod = Symbol('method')

export { useMiddlewares, controller, requestPath, requestMethod }
